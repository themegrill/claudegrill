{
  "_comment": "Local dev MCP config. In CI the workflow writes its own headless version. Issue filing needs no MCP server: it goes through `gh` and scripts/file-issue.mjs.",
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--isolated", "--browser", "chromium"]
    }
  }
}
